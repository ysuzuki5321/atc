D
  →k回前と同じ手は出せない
    →k個離れているところが違う場合は両方得点出来る
    →同じ場合は連続している数/2個+奇数であれば1得点出来る
      →これを計算すればよい
E
  →ゲストiのパワーはai
    →握手をm回出来る
      →パーティ全体の幸福度を上げる
        →１．左手で手を握るxと右手で手を握るｙを決める
        →２．実際にこれら二本の手を握ることで幸福度がAx+Ay上がる
      →ただし全く同じ握手を二回行ってはいけない
        →xkとykと手を握ったとする。
          →この時(xp,yp)=(xq,yq)を満たすようなp,q(1<=p<q<M)が存在しない
            →同じ組み合わせがないだけ
      →M回の握手を行った後の、最終的な幸福度の最大を求めよ
    →1<=M<=N^2
    →1<=N<=10^5
      →llで処理せよ
      →N^2の組み合わせを作るにはNがでかすぎる
        →でかい方から処理をしたい
        →DPではないだろう
          →貪欲でqに入れての処理も出来ない、Mがでかすぎる
            →34,33,19であれば
              →34+34,34+33,33+34で次は33+33がでかい、しかしその次に来るのは34+19であくまでも33はまだ使われない
                →ソートしてまず、x1+x1をする
                  →x1がある状態でx2を使用する場合は
                    →(x1+x2)*2を足す、ここでMに達している場合は終了
                      →x2+x2をキープする(qに入れておく)
                        →x2をキープする(x1,x2)がキープされている状態
                          →x3まで進む
                            →x1+x3<x2+x2である場合x2+x2を足しqから削除する
                            →違う場合はそのまま
                              →x3+x3をキープする
                              →(x1+x3)*2+(x2+x3)*2つまり4回分処理出来る
                                →ここでMを超えているか判定する
                                →超えていればO(n)で計算して終了する
                                →超えていない場合2(x1+x2+2x3)を計算して回数に4を足す
                                  →(x1+x4)*2+(x2+x4)*2+(x3+x4)*2=
                                  →違う、x1を集中的に足しこむ方が有利なのである
                                    →優先度的にはx1を足しこむ上で自分の数字*2が有利な分は入れていけば良い
                                      →完全に間違いa<b<c<dとなっていてa+d < b+cとなっている場合が完全に考察から抜け落ちていた
                                        →2分探索でやるとのことである
                                  
                →それぞれの手が何回出てくるかを求めてO(n)で処理するのが正解か
        →累積しておく必要もありそうである
      →ある組み合わせを使用する場合は、同じゲストでない限り2回出てくる
  再チャレンジ
    ax+ay増える
      xp,yp=xq,yqのようなp,qが存在しないという条件を満たす
        M回の握手を行った後最終的な幸福度はさいだいでいくらに出来るか
          2分探索で求められたように思う
            組み合わせの大きい方から取得するわけだが1<=N<=10^5であるため
              組み合わせを全て配列にすることは出来ない
                1<=ai<=10^5であることを活かす
                  最大でも2*10^5
                    それぞれの手がいくつあるか求められる
       tを越える手がいくつ作れるかである
        この計算はo(n)で可能
            
      
        
  
  
  
  
