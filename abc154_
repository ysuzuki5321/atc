数学系はすぐググる

E - Almost Everywhere Zero
  1<N以下の整数
    １０進法で表したときに0でない数がちょうどk個あるようなものの個数を求める
      桁DPの予感
        1<=k<=3
          kが大きくない
            
F - Many Many Paths
  x方向に正1、ｙ方向に正1移動できる
  f(r,c)=(0,0)から(r,c)まで上記の操作を繰り返して到達する経路の個数
    整数r1,r2,c1,c2が与えられる
      r1<=i<=r2かつc1<=j<=c2を満たす全ての整数の組(i,j)に対するf(i,j)の総和を求め(10^9+7)で割った余りを計算する
    1<=r1<=r2<=10^6
    1<=c1<=c2<=10^6
  iに対してjを計算すると最大10^6^2という計算量
    ある一つの組み合わせに対する総和であればCOM(i,j)で良い
      iとi+1に対して組み合わせの数がどれだけ増えるのか分かればすぐ求められる
       パスカルの三角形の和を使用しようとしたが難しい
        おおもとの場所の和が出せない
          というか出せたら終わりの問題でもある
            アンバランスに組み立てられるか
              2項係数の部分的な総和をO(1)で求められれば行ける
                うーん、難しい
                  累積させるのが難しい
                    増え方がスライドしている
                      ここをキーとして考えられるか？
                        2項係数の矩形和は
                          c(r,0)+c(r,1)+c(r,2)+,...,+c(r,k)=c(r+1,k)で求められる！！！！！！！！！！！
                  
