D - Maze Master
  →1<=h,w<=20
    →上下左右に隣接するマスに移動することができる
    →壁'#'へは移動できない
     →最小移動回数が最大になるようにs,gを定める
      →400*400なのでゴリ出来る
      
E - Max-Min Sums
  →有限個の整数からなる集合Xに対してf(x) = maxX-minXと定義
  →N個の整数A1,,,,,,Anが与えられる
    →このうちK個を選びそれらからなる集合をSとする
      →選び方はnCk通りある、その全てについてf(S)の合計を求める
  →1<=N<=10^5
  →1<=k<=n
  →|Ai|<=10^9
  →負の数がある
  →ソートはしてよい
    →あるｋ個以上離れた二つの数字を選んだ時にその中のパターンは
      →0<=i<j<=nを選んだとしたとき
      →離れているインデックス数をyとするとy-2Ck-2通り出来てy-2Ck-2*(aj-ai)となる
        →二つを選べれば簡単であるが、そうすると(n^2)になる
          →y-2Ck-2*(aj-ai)なので
            →y-2Ck-2*aj-y-2Ck-2*aiに分けられる
              →jに対してiを決めない場合
                →jCk-1でjを最大として選んだ時の和が求められる
        →jに対するループはkから開始する
          
F - Enclose All
  →N個の点xi,yiが与えられる
    →2<=N<=50
    →0<=xi<=1000
    →0<=yi<=1000
    →これらのすべての点を内部、または円周上に含む円の半径の最小値を求めよ
  →Nは小さい
    →minXとmaxXで横の長さの最低値が出る
    →minYとmaxYで縦の長さの最低値が出る
      →問題としてはそれらの最低値の外側に出てしまう点が存在する
        →
  →苦手系な問題である
  →円周上の点は一つではありえない
    →2分探索か？
      →それぞれの点からの領域を半径tで取る
        →全ての点からの域が重なった時、その半径tは全ての点を囲む円の半径として使える 
        →このようなtの最小値が答え
        →判定は？
          →それぞれの二点の距離が2tに収まっているかどうか
            →判定はO(n^2)で可能
              →二分探索であれば              
                →O(logp(距離の最大)*n^2)
                  →WAである微妙に考え方が違うのがあるようだ
                    →
                
 
